services:
  - type: web
    name: website-fetcher
    env: node
    plan: standard
    buildCommand: npm install --omit=dev
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
